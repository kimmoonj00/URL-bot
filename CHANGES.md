# 2026-09-01 작업 내역

## [1] 병렬화 (크롤링 + GPT 추출)

크롤링은 기존에 URL을 하나씩 순서대로 처리했는데, 이제 최대 3개를 동시에 브라우저 탭으로 열어서 처리한다. Chrome은 1개(같은 세션) 안에서 탭만 여러 개 쓰기 때문에 봇 차단 위험이 늘어나지 않는다.

GPT 추출도 기존에는 URL마다 순서대로 기다렸는데, 이제 최대 5개를 동시에 날린다. GPT 호출은 네트워크 대기가 대부분이라 동시 처리해도 충돌이 없다.

| 구간 | 변경 전 | 변경 후 | 단축 |
|------|--------|--------|------|
| 크롤링 | 227.6초 | 136.0초 | 약 40% |
| GPT 추출 | 25.3초 | 6.4초 | 약 75% |

---

## [2] GPT 컨텍스트 길이 상향

GPT에 넘기는 페이지 텍스트 최대 길이를 10,000자 → 20,000자로 늘렸다. 긴 규격 테이블이나 상세 설명이 잘려나가던 문제 개선. 응답 토큰도 1,500 → 3,000으로 늘렸다.

---

## [3] 제조원 추출 추가

기존에는 상품명·모델번호·규격만 뽑았는데, 이제 제조사/브랜드도 함께 추출한다. GPT 프롬프트 스키마에 `manufacturer` 필드 추가, 결과에 `제조원` 포함, 화면에도 표시된다.

---

## [4] GPT 예시(Few-shot) 보강

GPT는 규칙 설명보다 예시를 더 잘 따르는 경향이 있다. 기존 예시 2개에 `제조원` 필드를 추가하고, 제조사가 명시된 3번째 예시(Swagelok 스타일)를 새로 넣었다. 특히 `제조원` 추출 정확도 향상 목적.

---

## [5] URL 모델번호 힌트 주입

Swagelok(`T-8M3-1`)나 Festo(`34411`)처럼 URL 경로 자체에 모델코드가 들어있는 사이트가 있다. 이 정보를 GPT 프롬프트에 힌트로 넘겨서 모델번호 추출 정확도를 높였다. URL에 모델코드가 없으면 "(없음)"으로 표시되고 GPT가 무시한다.

---

## URL 조정 기능 — 미적용 (롤백)

잘린 URL이나 공백 포함 URL을 자동으로 고쳐서 재시도하는 기능을 구현했다가 롤백했다. 이유:
- 테스트한 사이트들(다나와, 지멘스, navimro 등)이 전부 불완전한 파라미터를 무시하고 200을 반환해서 fix 로직이 개입할 일이 없었다.
- 공백 포함 URL은 GUI에서 실행 버튼이 애초에 비활성화되어 백엔드까지 도달하지 않는다.
- 4xx가 실제로 나는 경우는 URL 자체가 잘못된 것인데, 이때 파라미터를 자르면 목록 페이지 같은 엉뚱한 곳으로 이동할 위험이 있다.
