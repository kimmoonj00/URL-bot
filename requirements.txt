playwright>=1.45.0
# paddlepaddle는 3.0.0으로 고정한다. 이후 패치 버전(예: 3.3.1, 현 latest)에서는
# Windows CPU 환경에서 PP-OCRv5 det/rec 추론 중 재현되는 네이티브 크래시
# (NotImplementedError: ConvertPirAttribute2RuntimeAttribute ... onednn_instruction)
# 버그가 있어 paddleocr==3.0.0과 같은 시기에 나온 3.0.0으로 맞춘다.
paddlepaddle==3.0.0
paddleocr==3.0.0
Pillow>=10.0.0
numpy
ollama>=0.6.0
# 주의: pykospacing은 TensorFlow를 딸려와 numpy를 2.x로 올려버려서
# paddlex(numpy==1.24.4 고정)와 충돌한다. 절대 추가하지 말 것.
kiwipiepy
